import styled from 'styled-components';

export const StyledButton = styled.button`
  border: none;
  padding: 10px 15px;
  background-color: black;
  color: white;
  &:hover {
    opacity: 0.85;
    cursor: pointer;
  }
`;
